"use strict";

function _interopDefault(ex) {
  return ex && "object" == typeof ex && "default" in ex ? ex.default : ex;
}

Object.defineProperty(exports, "__esModule", {
  value: !0
});

var input = require("@arch-ui/input"), React = require("react"), React__default = _interopDefault(React);

const FloatFilterView = ({onChange: onChange, filter: filter, field: field, innerRef: innerRef, value: value}) => {
  if (!filter) return null;
  const placeholder = field.getFilterLabel(filter);
  return React__default.createElement(input.Input, {
    onChange: event => {
      const value = event.target.value;
      /^-?\d*\.?\d*$/.test(value) && onChange(value);
    },
    ref: innerRef,
    placeholder: placeholder,
    value: value
  });
};

exports.default = FloatFilterView;
