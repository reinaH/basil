"use strict";

function _interopDefault(ex) {
  return ex && "object" == typeof ex && "default" in ex ? ex.default : ex;
}

Object.defineProperty(exports, "__esModule", {
  value: !0
});

var input = require("@arch-ui/input"), React = require("react"), React__default = _interopDefault(React);

const DecimalFilterView = ({onChange: onChange, filter: filter, field: field, innerRef: innerRef, value: value}) => {
  const placeholder = field.getFilterLabel(filter);
  return React__default.createElement(input.Input, {
    onChange: event => {
      const value = event.target.value;
      onChange(value.replace(/[^0-9.,]+/g, ""));
    },
    ref: innerRef,
    placeholder: placeholder,
    value: (value => "string" == typeof value ? value : "number" == typeof value ? String(value) : "")(value)
  });
};

exports.default = DecimalFilterView;
