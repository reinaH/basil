"use strict";

function _interopDefault(ex) {
  return ex && "object" == typeof ex && "default" in ex ? ex.default : ex;
}

Object.defineProperty(exports, "__esModule", {
  value: !0
});

var React = require("react"), React__default = _interopDefault(React), input = require("@arch-ui/input");

const VirtualFilterView = ({onChange: onChange, filter: filter, field: field, innerRef: innerRef, value: value}) => {
  if (!filter) return null;
  const placeholder = field.getFilterLabel(filter);
  return React__default.createElement(input.Input, {
    onChange: ({target: {value: value}}) => {
      onChange(value);
    },
    ref: innerRef,
    placeholder: placeholder,
    value: value
  });
};

exports.default = VirtualFilterView;
