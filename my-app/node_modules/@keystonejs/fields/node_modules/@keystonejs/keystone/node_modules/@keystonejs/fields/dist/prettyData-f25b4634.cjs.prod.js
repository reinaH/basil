"use strict";

function _interopDefault(ex) {
  return ex && "object" == typeof ex && "default" in ex ? ex.default : ex;
}

var React = require("react"), React__default = _interopDefault(React);

const stringify = data => {
  const dataWitoutTypename = JSON.parse(JSON.stringify(data), (key, value) => "__typename" === key ? void 0 : value);
  return JSON.stringify(dataWitoutTypename, null, 2);
};

var PrettyData = ({data: data}) => {
  if (!data) return null;
  let prettyData = "";
  return prettyData = "string" == typeof data || "number" == typeof data ? data : React__default.createElement("pre", null, stringify(data)), 
  prettyData;
};

exports.PrettyData = PrettyData;
