'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

function _interopDefault (ex) { return (ex && (typeof ex === 'object') && 'default' in ex) ? ex['default'] : ex; }

var React = require('react');
var React__default = _interopDefault(React);
var filters = require('@arch-ui/filters');

const inputMap = {
  is_true: true,
  is_false: false,
  is_null: null
};

const CheckboxFilterView = ({
  onChange,
  filter,
  value
}) => {
  const handleChange = newValue => {
    onChange(inputMap[newValue]);
  };

  const textValue = value === true ? 'is_true' : value === false ? 'is_false' : 'is_null';
  if (!filter) return null;
  return React__default.createElement(filters.RadioGroup, {
    onChange: handleChange,
    value: textValue
  }, React__default.createElement(filters.Radio, {
    value: "is_true"
  }, "True"), React__default.createElement(filters.Radio, {
    value: "is_false"
  }, "False"), React__default.createElement(filters.Radio, {
    value: "is_null"
  }, "Not set"));
};

exports.default = CheckboxFilterView;
