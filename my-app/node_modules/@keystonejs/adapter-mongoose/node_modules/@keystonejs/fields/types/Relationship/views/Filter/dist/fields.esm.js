import '@babel/runtime/helpers/defineProperty';
import '@babel/runtime/helpers/objectWithoutProperties';
import '@emotion/core';
import React__default from 'react';
import '@babel/runtime/helpers/extends';
import '@apollo/client';
import '@arch-ui/select';
import 'react-select';
import 'intersection-observer';
import { R as RelationshipSelect } from '../../../../../dist/RelationshipSelect-6728caa2.esm.js';

const EventCatcher = ({
  children
}) => React__default.createElement("div", {
  onClick: e => {
    e.preventDefault();
    e.stopPropagation();
  }
}, children);

const RelationshipFilterView = ({
  onChange,
  filter,
  field,
  value
}) => {
  const handleChange = option => {
    if (option === null) {
      onChange(null);
    } else {
      const {
        value
      } = option;

      if (value) {
        onChange(value.id);
      }
    }
  };

  if (!filter) return null;
  const htmlID = `ks-input-${field.path}`;
  return React__default.createElement(EventCatcher, null, React__default.createElement(RelationshipSelect, {
    field: field,
    renderContext: null,
    htmlID: htmlID,
    onChange: handleChange,
    value: value,
    isMulti: false
  }));
};

export default RelationshipFilterView;
