"use strict";

Object.defineProperty(exports, "__esModule", {
  value: !0
});

var core = require("@emotion/core");

const Image = ({alt: alt, src: src}) => core.jsx("div", {
  css: {
    alignItems: "center",
    display: "flex",
    height: 24,
    lineHeight: 0,
    width: 24
  }
}, core.jsx("img", {
  alt: alt,
  css: {
    maxHeight: "100%",
    maxWidth: "100%"
  },
  src: src
}));

var Cell = ({data: data}) => {
  if (!data) return null;
  const isImage = data.mimetype.includes("image"), truncatedFilename = data.filename.replace(data.id + "-", "");
  return isImage ? core.jsx(Image, {
    alt: truncatedFilename,
    src: data.publicUrl
  }) : core.jsx("div", null, truncatedFilename);
};

exports.default = Cell;
